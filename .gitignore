# ── 大文件：数据集 / 预训练权重 / 论文 PDF（不入库）──
datasets/
dps-checkpoint/
*.pt
*.pth
ZAPS.pdf

# ── 本地代理说明 / 工作流文档（不入库）──
AGENTS.md
CLAUDE.md
docs/

# ── Python 缓存 ──
__pycache__/
*.pyc
*.pyo

# ── 旧扁平结果目录（已被 experiments/ 取代）──
projects/results/

# ── 从服务器同步回来的根目录实验副本 ──
/experiments/

# ── 实验归档：重建图不入库，仅追踪 CSV / run.json / conclusion.md ──
projects/experiments/**/*.png

# DPS 官方代码(对照审查用),排除大文件
DPS/models/
DPS/results/
DPS/figures/
DPS/**/*.pt
DPS/**/*.part
DPS/**/*.pth
